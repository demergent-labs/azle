name: Release Branch Workflow

on:
    pull_request:
        branches:
            - release--bdemann.test.release

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - run: echo "Hello world!"

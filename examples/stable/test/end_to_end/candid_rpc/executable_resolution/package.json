{
    "dependencies": {
        "azle": "0.33.0-rc.0"
    },
    "scripts": {
        "pretest": "tsx test/pretest.ts",
        "test": "AZLE_CHECK_MEMORY_STATE_AFTER_TESTS=false AZLE_CHECK_GLOBAL_STATE_AFTER_TESTS=false jest"
    }
}

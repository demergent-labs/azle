service: () -> {
    getUserById: (text) -> (opt record {id:text; username:text}) query;
    getAllUsers: () -> (vec record {id:text; username:text}) query;
    createUser: (text) -> (record {id:text; username:text});
}

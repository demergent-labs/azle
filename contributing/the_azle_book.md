```bash
# For local editing

cd the_azle_book
mdbook serve --open
```

```bash
# For deployment
cd the_azle_book
mdbook build --dest-dir ../docs

# Then commit the changes to git and merge into main on GitHub
```

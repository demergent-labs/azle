{
    "scripts": {
        "test": "ts-node --transpile-only --ignore=false index.ts"
    },
    "dependencies": {
        "azle": "^0.18.5"
    },
    "devDependencies": {
        "fast-check": "^3.13.1"
    }
}

// This is a placeholder main.rs file for the azle-dev-tools crate.
// This crate exists solely to track Rust command-line tools used by Azle developers
// as dependencies, allowing them to be scanned by cargo audit, cargo deny, and dependabot.
// The tools are not actually used in this crate; they are just tracked as dependencies.

fn main() {
    println!("This crate is for dependency tracking only");
}